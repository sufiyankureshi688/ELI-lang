# FUNC ?name *body ENDFUNC
# Emits function body after a jump that skips it.
# Caller pushes args then does: CALL __name__

MATCH FUNC ?name *body ENDFUNC

CONSTRAINT
single(name)
is_name(name)
non_empty(body)

ELI

EMIT
J [end] [:__name___entry] {body} [:end]
